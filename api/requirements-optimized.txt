# Alternative: Explicit CPU-only PyTorch installation
# This version is much smaller (~500MB vs ~2-3GB for full PyTorch)

# Install CPU-only PyTorch (smaller build)
torch==2.1.0+cpu --index-url https://download.pytorch.org/whl/cpu
torchvision==0.16.0+cpu --index-url https://download.pytorch.org/whl/cpu

# Other dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pillow>=10.1.0
numpy>=1.24.3

